# الهمّة — ملخص المشروع (اقرأ هذا أولاً)

تطبيق تطوير ذاتي عربي محفّز (لهجة سعودية شبابية)، اسمه **«الهمّة»** (شعار نصي: «الهمّة حتى القمّة»)، يُرفع على منصة Manus.
> ملاحظة: الاسم القديم كان «مفكرة النجاح» وتغيّر للهمّة. مفتاح localStorage بقي `mufkirat_core_v1` (لا تغيّره).

## المعيار العام المتّفق عليه (يحكم كل صفحة — راجعه قبل أي تعديل)
- اللهجة: سعودية شبابية محفّزة، مضحكة بلمسات خفيفة، ومحترفة بنفس الوقت (مو ركيكة ولا سطحية).
- المحتوى مبتكر ومحفّز على الإبداع والإنجاز — ما هو نصائح عامة مكرّرة.
- خالٍ من الأخطاء الإملائية والنحوية العربية، وخالٍ من الأخطاء البرمجية (`tsc` نظيف دائماً).
- صفر ثغرات أمنية (راجع قواعد الأمان أدناه بكل صفحة تُلمس).
- خفيف وسريع (تجنّب حسابات ثقيلة بلا `useMemo`، صور مضغوطة، lazy loading).
- سهل الاستخدام — لا تعقيد أو خطوات زايدة لإنجاز مهمة بسيطة.
- تصميم جمالي متّسق (توكنز الألوان والتدرّجات فقط، لا قيم ثابتة).
- الإحساس العام: التطبيق «لعبة» (نقاط، مستويات، إنجازات، مفاجآت) لكنها لعبة تحفّز فعلياً للنجاح الحقيقي، مو تسلية فاضية.

## التقنية
React 18 + TypeScript + Vite 8 + wouter (توجيه) + localStorage (مفتاح `mufkirat_core_v1`).
PWA مفعّل (manifest + service worker). البناء: `npm run build` · التطوير: `npm run dev`.

## القواعد الصارمة (لا تُكسر)
- **النواة المركزية `src/core/useCore.tsx`** = المصدر الوحيد لكل الحالة والنقاط (XP). لا حالة مستقلة في أي صفحة.
- **أمان**: ممنوع `innerHTML`/`dangerouslySetInnerHTML` — نص `{متغير}` فقط. كل معرّف `crypto.randomUUID()`. تنظيف المدخلات عبر `clean()` (≤200 حرف) — لكن الحقول الحيّة (إدخال أثناء الكتابة) تُنظَّف عند الحفظ لا كل ضغطة.
- **الألوان**: صفر لون ثابت — الكل `var(--..)` من `:root` في `src/styles/global.css`.
- **الحذف** عبر `ConfirmDialog`. لا `prompt`/`confirm`.
- **الاحتفال** (`fireConfetti`) للإنجازات الكبيرة فقط (هدف مكتمل، رقم شخصي، حفظ جزء، إكمال يوم/قسم، ٣ شكر).
- اللهجة سعودية شبابية للرسائل — لكن **الآيات وصيغة العهد تبقى فصحى**.
- صيغة الخطاب حسب الجنس (`profile.gender`): أحسنت/أحسنتِ.

## البنية
- `src/core/useCore.tsx` — النواة (XP، الملف، السلسلة، الثيم، كل بيانات الأقسام).
- `src/core/useWorkoutTimer.tsx` — مؤقت بـ timestamp.
- `src/components/` — XPBar, BottomNav, BackButton, ConfirmDialog, Confetti.
- `src/pages/` — الصفحات (محمّلة كسولاً عبر lazy في App.tsx).
- `src/data/workoutDays.ts` — بيانات جدول الكابتن سعود الستة.
- `src/pwa.ts` — تسجيل SW + التثبيت.

## الصفحات والمسارات
الرئيسية `/` · الـhub `/more` · الروتين `/routine` · الأهداف `/goals` · المزاج `/mood` ·
الملاحظات+الشكر `/notes` · القرآن+التقويم `/quran` · (النوم مدمج في الروتين المسائي `/routine`، ودائرة التواصل مدمجة في `/occasions`) ·
تطوير الذات (هوية+دستور+مراجعة) `/self-dev` · إنجازاتي (سلسلة+معرض+محطات) `/achievements` ·
التحليلات `/analytics` · العهود+صندوق الزمن `/pledges` · المصاريف `/expenses` ·
الوجبات `/meals` · التمارين (كابتن+جدولي+إرشادات) `/workouts` · الإعدادات `/settings`.
بوابة: تسجيل دخول → شاشة ترحيب (onboarding) → التطبيق.

## تبويبات الـhub
☀️ حياتي · 🔥 التزامي · 🌱 تطوّري · 🏆 فخري · 🛡️ العهود (بارز).

## الحالة الحالية (محدّثة — آخر جلسة)
كل الصفحات مكتملة. البناء نظيف (`tsc + vite`). ملف الرفع لـManus محدّث: `C:\Users\asus\Desktop\alhimmah-app.zip` (عبر `git archive HEAD`).
⚠️ **الفرع المحلي متقدّم بـ38 commit عن `origin/main` ولم يُرفع** — رمز GitHub الحالي **قراءة فقط** (انكشف PAT سابقاً)؛ المستخدم لازم ينشئ رمزاً جديداً بصلاحية Contents:write ويسوي `gh auth login` ثم `git push`. لين ذلك، النشر عبر رفع الـZIP لـManus.

### أُنجز في جلسة 2026-06-30 (تخصيص + أيقونات موحّدة + خطوط):
- **بوستات محفّزة باسم المستخدم**: دالة `personalize(text,name)` في `vibes.ts` تستبدل `{name}`. مطبّقة على بطاقات الترحيب (`welcomeMessages.ts`) + قفل اليوم + بانرات السحبة/الحصاد (`vibes.ts`). الاسم = `profile.name` ← `nickname` ← «بطل». اللقب في باقي الأماكن لم يتغيّر.
- **توحيد كل الأيقونات** (متمركزة `contain`/`center`، ملف webp مستقل لكل وحدة): حُوّلت من السبرايت إلى ملفات مستقلة: hub→more · analytics · goals · meals · workouts · expenses · achievements · pledges · home · settings · routine · selfdev. + أيقونة جديدة `occasions.webp` (كلاس `.ic-occasions`) للعلاقات (الشريط+الـhub). **`.ic-mood` يستخدم نفس `occasions.webp`** (بطلب المستخدم). الباقي على السبرايت (لا ملف مستقل): `habits`/`sleep`/`timecapsule`. الملفات الجديدة جت من المستخدم (4/5/8/10/11 → أعيدت تسميتها).
- **اسم القسم**: «نسختي الأفضل (الهوية والمراجعة)» → «نسختي الأفضل».
- **الخطوط**: حجم الأساس `18→19px` + خيار حجم ثالث «أكبر» (`fontScale: 'xlarge'`, zoom 1.26). **اختيار نوع الخط** (`fontFamily` بالنواة + `data-fontfamily` على root + تجاوز `--font-ui`): ٧ خطوط محلية — Tajawal(افتراضي)/Cairo/Almarai/Changa/El Messiri/IBM Plex/Amiri. ملفات woff2 حُمّلت من fontsource CDN إلى `public/fonts/`. واجهة الاختيار في `AppearanceSection.tsx` (كل زر يعرض بخطه، يلتف).

### أُنجز في جلسة 2026-06-30 (إصلاح الكاش + أيقونة فضفضة + ترتيب الأسبوع):
- **إصلاح «الكاش الكثير» / الشاشة البيضاء جذرياً** — كان الـSW السبب:
  - `src/pwa.ts`: الـSW يُسجَّل في **الإنتاج فقط** (`import.meta.env.PROD`)؛ في التطوير يُلغى أي SW قديم ويُمسح الكاش تلقائياً → خلاص ما يحتاج حذف `node_modules/.vite` يدوياً.
  - `public/sw.js`: استراتيجية جديدة — **HTML شبكة-أولاً** (النسخة الجديدة تنزل دايماً)، باقي الموارد **stale-while-revalidate**. رُفع الإصدار `v2 → v3`.
  - أُضيف `src/vite-env.d.ts` (`/// <reference types="vite/client" />`) — كان ناقصاً ويكسر `tsc`.
- **أيقونة «فضفضة» مستقلّة**: `.ic-notes` صار `background-image: url('/icons/notes.webp')` بدل قطعة السبرايت — المستخدم يحفظ صورته في `public/icons/notes.webp`. (باقي أيقونات الشريط لسّه داخل `icons-sprite.webp`.)
- **«نشاط الأسبوع» (Home.tsx)**: يبدأ من **الأحد** (أسبوع ثابت Sun→Sat بدل آخر ٧ أيام متحرّكة) ويُعرض من اليمين لليسار (RTL، الأحد يمين). الأيام القادمة من الأسبوع تظهر ٠٪.

### أُنجز في جلسة 2026-06-30 (محتوى تحفيزي + علاقات + ترحيب):
- **صفحة العلاقات** (`Occasions.tsx`): «نوع المناسبة» صار حقل كتابة حر (مثال «زواج، تخرّج») بدل القائمة، واسم الشخص بدون مثال. تبويب «دائرة التواصل» → **«أهلك وربعك»**. كل شخص له: 📞 آخر اتصال + 🗓️ موعد الاتصال المجدول. **زر السماعة 📞** = «تم الاتصال» بتاريخ اليوم (يعيد استخدام `toggleRelation`)، وزر 🗓️ يفتح `datetime-local` للجدولة. النواة: حقل `Relation.scheduledAt` + `scheduleRelationCall` (و`toggleRelation` يمسح الموعد عند الاتصال).
- **بنك المحتوى المركزي** `src/data/vibes.ts` (لهجة شبابية): جرعات لكل قسم + رسائل قفل اليوم + بانرات. ومكوّن `src/components/Dose.tsx` يعرض جرعة عشوائية ثابتة بالجلسة.
- **جرعات عشوائية داخل ٨ صفحات**: النادي/دراهمي/أكلي/نوري/المود/كلمتي/بطولاتي/تحدياتي (`<Dose section=".." />` بدل النص الثابت).
- **بطاقة «قفل اليوم»** بالرئيسية: تظهر مرة باليوم عند إكمال ١٠٠٪ من الروتين (عنوان عشوائي + كونفيتي عبر effect على `dayPopup`). حارس `localStorage` لمرة واحدة.
- **بطاقة ترحيب عشوائية** `src/data/welcomeMessages.ts`: تظهر بالرئيسية أول الجلسة، قابلة للإغلاق (`sessionStorage`)، خط IBM Plex.
- **بانرات داخل التطبيق** (تشتغل بدون Push Server): «السحبة» (غياب ٣ أيام، يعتمد `alhimmah_last_seen`) + «الحصاد» (الخميس/الجمعة/السبت، مرة بالأسبوع). نصوص الإشعارات اليومية في `notificationScheduler.ts` رُفعت لنبرة شبابية.
- **توست إنجاز كل مهمة**: دمج عبارات شبابية في تناوب `XPToast` (CHEERS) — يطلع مع أي كسب نقاط، بدون نظام موازي.
- **استبدال «وحش» → «مبدع»** في كل المحتوى (مستوى النواة الرابع `LEVELS`، وسام الشهر، الجرعات، قفل اليوم، التوست، إشعار الصباح). «الشنب» غير موجودة بالكود؛ لو اعتُمد وسامها لاحقاً يكون اسمه «الهامور المالي».
- **٤ أوسمة جديدة** في `Badges.tsx` (تُحسب محلياً): المسيطر (سلسلة ١٤ يوم) · الشرارة (٥ تمارين `workoutLogs`) · الهامور المالي (`expenses`≥١٠ وصافي موجب) · النسخة المحدثة (`weeklyReviews`>٠) — صارت ١٦ وسام.
- ⚠️ بيئي: حذف `node_modules/.vite` لزم **مرتين** لإصلاح شاشة بيضاء («Invalid hook call»/نسختين React في كاش التطوير) — يتكرر عند التعديل والخادم شغّال؛ البناء الإنتاجي سليم دائماً.

### أُنجز في جلسة 2026-06-30 (الشريط + الخطوط + أداء):
- **الشريط السفلي** (`BottomNav.tsx`) أُعيد ترتيبه وتسميته: الرئيسة→**«رحلتك للنجاح»** · `/notes`→**«فضفضة»** · `/quran`→**«نور حياتي»** · `/occasions`→**«الغالين»** · المزيد. (الأهداف انشالت من الشريط، تبقى بالـhub.)
- **أيقونة القرآن** الجديدة (مصحف+تقويم): `public/icons/quran.webp` (34KB) — `.ic-quran` في `global.css` صار `background-image` بدل قطعة السبرايت (تنطبق بالشريط والـhub). حُذفت نسخة png الزايدة.
- **تخفيف صفحة المصاريف** (`Expenses.tsx`): غُلّفت كل الحسابات المشتقة بـ`useMemo` (allCats / الإحصائيات والمقارنة الشهرية / catTotals+pieEntries+pieSum+piePaths) — قبل كانت تتعاد كل re-render (كل ضغطة كيبورد). سلوك مطابق، مكسب أداء بحت. tsc نظيف.
- **خطوط جديدة محلية** (بدون طلب خارجي حاجب، مثل Amiri): الواجهة **Tajawal** (`--font-ui`، 400/500/700)، والعناوين+اسم التطبيق **IBM Plex Sans Arabic** (`--font-display` الجديد، 600/700) — مطبّق على `.page-hero-title`/`.home-hero-greet`/`.auth-slogan`/`.auth-title`. ملفات woff2 عربية في `public/fonts/` + preload لـTajawal في index.html. ⚠️ الخط القديم `Alyamama` في `--font-ui` كان **وهمياً** (ما له ملف، كان يقع لـSegoe UI).
- **حجم الخط** الأساس: `html { font-size: 18px }` (كان 16 ضمنياً).

### أُنجز في جلسة 2026-06-29 (ميزات + هوية + لهجة):
- **هوية بصرية**: شعار جديد ذهبي (`public/logo.png` شفاف) + أيقونات من الشعار على خلفية غامقة (`icon-192/512.png`, `apple-touch-icon.png`) — manifest+index.html+sw.js محدّثة، حُذف `icon.svg` القديم.
- **ثيمات جديدة**: «أخضر سعودي» (#006c35، الافتراضي الآن) و«ذهبي» — في `AccentName` + `global.css` + `AppearanceSection`.
- **هوية سعودية**: تاريخ هجري بالرئيسية، رمز الريال ﷼ بالمصاريف، مناسبات سعودية (اليوم الوطني/التأسيس)، ألقاب مستويات بروح (طاير→…→قمة الهمّة 🏔️ في `LEVELS`).
- **أصوات نجاح اختيارية**: `src/core/sound.ts` (Web Audio، بدون ملفات) + `soundOn` + مفتاح بالإعدادات (مطفأ افتراضياً). اهتزاز خفيف عند إنجاز مهمة.
- **توهّج ذهبي** على هيرو الرئيسية (نهاري/ليلي).
- **قوالب احترافية** `src/data/templates.ts` + صفحة `/templates` + `core.applyTemplate` (أهداف/روتين/تحديات/ميزانية).
- **أوسمة** `src/pages/Badges.tsx` (تبويب في الإنجازات، ١٢ وسام تُحسب محلياً).
- **بحث شامل** `src/pages/Search.tsx` (`/search` + زر 🔍 بالهيرو).
- **تنبيه ذكي سياقي** بالرئيسية (Home.tsx `smartNudge`: سلسلة→مزاج→نوم→امتنان) + إشعار السلسلة مشروط (App.tsx).
- **زر الحفظ باللقب**: `saveLabel(nick)` = «كفو يا {اللقب} أنا قدها» (nickname||name||'بطل') في المزاج/النوم/الهوية/التمارين/المراجعة. + صفحة المزاج: حُذفت «ميرادك».

### أُنجز في جلسة فحص الجودة (QA) — 2026-06-29:

### أُنجز في جلسة فحص الجودة (QA) — 2026-06-29:
- **فحص جودة شامل** للتطبيق وهو شغّال (preview) + إصلاحات أمان البيانات:
  - **ErrorBoundary** (`src/components/ErrorBoundary.tsx`) يلفّ المسارات في `App.tsx` — لا مزيد من الشاشة البيضاء عند انهيار أي صفحة (رسالة ودّية + زر رجوع). تم التحقق حياً.
  - **تنبيه امتلاء التخزين**: `useCore` ما عاد يبلع فشل `localStorage.setItem` بصمت — حقل `storageFull` + `dismissStorageWarning` + `exportData`، وشريط `src/components/StorageBanner.tsx` (أحمر، فوق التطبيق، زر تصدير فوري). تم التحقق حياً.
  - **تكامل XP**: `toggleRoutineDone` صار يخصم 5 عند إلغاء التحديد (متماثل، مقيّد بـ`Math.max(0,…)`، مع مزامنة `xpRef`) — كان يبقي النقاط لمهمة غير منجزة.
  - **تصغير صور ضخمة**: `logo.png` 1.8م←84ك، `routine.webp` 1.8م←13ك، `analytics.webp` 2م←11ك (192px)، وحذف `public/icons/23333.png`. (~14م توفير).
- **قرارات QA مقصودة (لا تُغيَّر بلا سبب):** السلسلة (`markStreakToday`) تُحدَّث عند إنجاز **أي** مهمة روتين — مقصود لتحفيز الاستمرار. + إلغاء التحديد لا يكسر السلسلة.
- ⚠️ **ملاحظات QA متبقّية (للمستقبل):** الصور تُحفظ base64 داخل localStorage (يُفضّل رابط Manus)؛ منطق التاريخ يعتمد ساعة الجهاز (حافة تلاعب بالسلسلة)؛ لا مزامنة عبر الأجهزة (محلي بالكامل — يُحلّ بربط Manus).

### أُنجز في جلسة 2026-06-29 (سابقاً):
- **ألقاب شبابية + جرعة محفزة** لكل صفحة (العنوان = اللقب + بطاقة `.intro-card` «💊 الجرعة المحفزة»): روتيني الصح، مودي اليوم، فضفضة وامتنان، وردي اليومي، وعد الحر دين، قم لـ جيم، دراهمي، أكلي الصح، إنجازي الأسبوعي، منصة التتويج، نسختي الأفضل، الكواليس والأمان. مطبّقة أيضاً على بلاطات الرئيسية وروابط الـhub.
- **زر حفظ محفّز** `src/core/saveLabel.ts` («كفو عليّ يا زيني/زينة أنا قدها» حسب الجنس) على أزرار الحفظ الرئيسية كاملة العرض (المزاج/النوم/الهوية/صمّم تمارينك/مراجعة الأسبوع).
- **مواقيت + تذكير الصلاة**: `src/core/prayerTimes.ts` (مكتبة `adhan`، طريقة أم القرى، افتراضي مكة، تحميل كسول) + بطاقة في صفحة القرآن + `schedulePrayerNotifications` + حالة `prayerNotif`/`prayerCoords` + إعادة جدولة في `App.tsx`.
- **رؤية ذكية ترابطية** بالتحليلات (أثر النوم على الطاقة) + خط الاتجاه يحترم الفلتر (7أيام/4أسابيع/12شهر) + «أنشط أيام أسبوعك» + متوسط الطاقة. **إصلاح RTL**: الرسم الخطي صار الأقدم يمين←الأحدث يسار. **مرصد الانسجام موسّع** (8 مجالات: الصحة+الوجبات، الإنتاجية+الملاحظات+النِيّة، الطموح=الأهداف+العهود) + إصلاح العلاقات بـ`Relation.contactedDate`.
- **سلسلة لكل عادة**: `RoutineTask.history[]` + شارة «🔥N» في الروتين. **إعادة ترتيب** ▲▼ (`moveRoutineTask`/`moveGoalStep` — بديل DnD غير المدعوم باللمس).
- **Wrapped كصورة**: `html-to-image` (toPng) زر «احفظها كصورة» (الأزرار خارج البطاقة).
- **المصاريف**: تعديل الفئة (`updateCustomCategory` — ينقل الميزانية والمصاريف) + عناصر إضافة سريعة قابلة للتعديل (`quickShopItems` + add/update/remove). **الأهداف**: `editGoalCategory`.
- **دمج**: النوم داخل الروتين المسائي (`/sleep` محذوف، SleepRelations `embedded`)، دائرة العلاقات داخل `/occasions` (تبويب «دائرة التواصل»). بطاقة المناسبة: حُذف «نوع العلاقة»، المثال «سعود».
- **تطوير الذات → «نسختي الأفضل»**. **«الكابتن سعود» → «المدرب سعود»** في كل مكان.
- **أداء**: تحميل كسول (lazy+Suspense) لتبويبات التمارين/الإنجازات/تطوير الذات. **خط Amiri محلياً** (`public/fonts/*.woff2` + `@font-face` + preload، أُزيل طلب Google Fonts). `loading=lazy` للصور.
- **لهجة سعودية**: تحويل الرسائل الفصحى المتبقّية عبر ~15 صفحة (مع إبقاء الآيات/العهد/التأمّل فصحى).
- ⚠️ **ملاحظة بيئية**: تثبيت npm والخادم شغّال يلخبط كاش Vite («Invalid hook call» وهمي) — الحل: حذف `node_modules/.vite` + إعادة تشغيل. البناء الإنتاجي غير متأثّر.
- ⚠️ **أمان**: رمز GitHub PAT (`ghp_...`) انكشف بالمحادثة — يحتاج المستخدم يلغيه وينشئ جديد.

### أُنجز سابقاً:
- **نِيّة اليوم (Daily Intention)**: `state.dailyIntention {date,text}` + `core.setDailyIntention` (تنظيف `clean`، +5 XP وكونفيتي أول مرة باليوم) + بطاقة بالرئيسية (`.intent-card`، لهجة سعودية) قبل شارة إنجاز اليوم.
- **إصلاح إشعارات**: إعادة جدولة `scheduleNotifications` تلقائياً عند فتح التطبيق (effect في `App.tsx` يعتمد على `notifMaster`+`notifItems`) — قبل كانت تتجدول فقط من زر الإعدادات. + زر طلب إذن في صفحة `/notifications` (`requestNotifPermission`).
- **تغيير الاسم** «مفكرة النجاح» → «الهمّة» في كل مكان (manifest, index.html, الرئيسية, hub, الإعدادات, الإشعارات, البومودورو, نص مشاركة Wrapped). الشعار النصي «الهمّة حتى القمّة» في الدخول والترحيب (`.auth-slogan`).
- **ترحيب حسب التوقيت + نجوم متلألئة** في هيرو الرئيسية (`.home-hero-greet`, `.hero-star`, `.home-hero.night`). الافتراضي للترحيب `'بطل'` (لتفادي «يا يا بطل»).
- **نظام تصميم موحّد**: مكوّن `src/components/PageHero.tsx` + توكنز تدرّجات في `:root` (`--grad-primary/deep/night/sunset/calm`) + كلاس `.page-hero`. مطبّق على: الإرشادات، صمّم تمارينك، السلسلة، النوم، الإعدادات.
- **نظام تحفيز على كل الصفحات**: مكوّن `src/components/XPToast.tsx` + `fireXP` مربوط داخل `addXP` في النواة (قمع واحد → توست بكل صفحة + كونفيتي عند صعود المستوى، عبر `xpRef` لتفادي ازدواج StrictMode).
- **تحدّي أسبوعي عشوائي**: `src/data/weeklyChallenges.ts` + `state.weeklyChallenge` + `core.weekly` + `core.completeWeeklyChallenge` + بطاقة بالرئيسية (`.weekly-card`).
- **تجميل لوني**: `--bg #f5f8f8`, `--border #e7edec`, `--text-secondary #6c7a7c`, ظلال أغنى، تدرّج محيطي على `#root`. أصلحت `--deep-2` (كان يشير لنفسه).
- **لهجة سعودية**: رسائل تأكيد الحذف (12 صفحة) + حالات فارغة + تلميحات (مثل «متأكد تبي تحذف...»). حُذفت مناسبة عيد الميلاد من Occasions.

## الأيقونات (مهم — محدّث)
- **أيقونة التطبيق**: `public/icon.svg` تدمج الشعار الذهبي على خلفية متدرّجة غامقة.
- **السبرايت**: استُبدل `icons-sprite.png` بـ`public/icons-sprite.webp` (43KB، 6×3 شفاف). الـCSS يشير لـ`url('/icons-sprite.webp')`. يستخدمه الشريط السفلي و`Hub`.
- **بلاطات الرئيسية (9)**: ملفات webp مستقلة في `public/icons/` (more, routine, goals, expenses, workouts, meals, analytics, pledges, achievements). العرض يدعم الصور عبر `icon.startsWith('/')`. `BottomNav` يدعم `img` أيضاً.
- **صور الكابتن**: `public/workouts/{push_a,pull_a,legs_a,push_b,pull_b,legs_b}.webp` — حقل `image` في `workoutDays.ts`، تُعرض أعلى كل يوم في `CaptainWorkout.tsx` (`.day-infographic`).

## النشر على Manus
- ملف الرفع جاهز: `C:\Users\asus\Desktop\alhimmah-app.zip` (بدون node_modules/.git/dist).
- برومبت Manus الكامل أُعطي للمستخدم (يبني + ينشر + يربط مصادقة/قاعدة بيانات لكل مستخدم بدل localStorage + رفع الصور لتخزين Manus).

## ملاحظات بيئية (هذه الجلسة فقط)
- HMR websocket يفشل أحياناً → أعِد التحميل يدوياً (Ctrl+Shift+R). أداة لقطة الشاشة (preview_screenshot) تتعلّق — اعتمد التحقق عبر DOM (`preview_eval`).

## المتبقّي (أفكار)
- ربط Manus (مصادقة + قاعدة بيانات + تخزين صور) — لم يُنفَّذ بعد (المستخدم يرفع الـZIP لـManus).

## تكامل Manus (مهم)
- المصادقة وقاعدة البيانات والصور تُربط من Manus — التفاصيل في `docs/04-تكامل-Manus-المصادقة-والصور.md`.
- اربط حفظ/تحميل `useCore` بقاعدة بيانات لكل مستخدم بدل localStorage.
- الصور: ارفعها لتخزين Manus واحفظ الرابط بدل base64.

## قاعدة العمل لتفادي التعارض
GitHub هو المصدر الوحيد. لا يعدّل طرفان (أنا + Manus) نفس الملفات بنفس الوقت.
