/* ===================================================================
   saveLabel.ts — نص زر الحفظ المحفّز الموحّد (باللقب اللي يحب يُنادى فيه)
   يُستعمل في أزرار الحفظ الرئيسية بكل الصفحات.
   مرّر اللقب: profile.nickname || profile.name || 'بطل'.
   =================================================================== */

export const saveLabel = (nick: string): string => `كفو يا ${nick} أنا قدها 💪`;
